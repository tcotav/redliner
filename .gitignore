__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
.DS_Store
.pytest_cache/
.ruff_cache/

# Per-run output is regenerable; the brief and phase state (state.json)
# are not, and belong with the manuscript they describe. Unanchored (**/)
# so this matches under any manuscript directory, not just the repo root.
**/.redliner/findings/
**/.redliner/canon/
