# Manuscript Brief: <title>

<!--
Written by /edaitor:intake, read by every edaitor editing pass before it
forms an opinion. Edit by hand any time; re-run /edaitor:intake to revise
it conversationally.

This template is generic across domains: the "Details" section is one
entry per item in the active domain's `domain.json` `brief_fields` list
(in that order, using each field's `label`), and "Draft stage" uses the
domain's `draft_stages`. Everything else on this page (deliberate
choices, known problem areas, off-limits, what the author wants) applies
to any kind of document and doesn't vary by domain.
-->

## Details

<!-- One bullet per domain.json brief_fields entry, in order, using its
     "label". Example, for fiction: -->

- **Logline:**
- **Genre / subgenre:**
- **Audience:**
- **Comps:**
- **Target length:**
- **POV:**
- **Tense:**
- **Structure:**

## Draft stage

**Stage:** <one of the domain's draft_stages names>

Severity implication for this stage:
<!-- Copy the matching draft_stages entry's "implication" verbatim, so
     each pass reads the rule rather than re-deriving it. -->

## Deliberate choices — do NOT report these as errors

<!-- The load-bearing section, and it isn't domain-specific: anything here
     is intentional; a pass that flags it is wrong, not observant. Be
     specific: "fragments in action scenes" not "some stylistic stuff". -->

-

## Known problem areas

<!-- What the author already knows is broken. Don't spend findings
     re-reporting these; do note if they're worse or differently broken
     than the author thinks. -->

-

## Off-limits

<!-- Not up for discussion. -->

-

## What the author wants from this pass

<!-- Specific worries. "Does the midpoint twist land?" is useful;
     "make it better" is not. -->

-
