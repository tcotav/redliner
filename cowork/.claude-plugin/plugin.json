{
  "name": "redliner-cowork",
  "description": "Layered manuscript-editing pipeline (fiction, design docs, and other long-form domains) for Cowork and other claude.ai-hosted surfaces: developmental and line editing, plus a cross-cutting continuity/canon-consistency layer, exposed via an MCP server instead of bin/-on-PATH.",
  "version": "0.5.0",
  "author": {
    "name": "redliner"
  },
  "mcpServers": {
    "redliner": {
      "command": "${CLAUDE_PLUGIN_DATA}/bin/redliner",
      "args": ["mcp"],
      "env": {
        "REDLINER_DOMAINS_DIR": "${CLAUDE_PLUGIN_ROOT}/domains"
      }
    }
  }
}
