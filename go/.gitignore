# Working copies capture_baseline.py builds fixtures into -- never
# checked in, regenerated by every run.
harness/.work/
harness/.selfcheck-a/
harness/.selfcheck-b/

# Built binary (Phase 6 decides where real committed binaries live --
# not the top-level go/ build output).
/redliner
/cmd/redliner/redliner

__pycache__/
.ruff_cache/
